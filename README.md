# Useful scripts in .ps1, .bat, .vbs and so on
_\_automate_ contains some attempts to automate installation procedures (e.g. installing and configuring IIS web server, installing SQL Server and so on)
